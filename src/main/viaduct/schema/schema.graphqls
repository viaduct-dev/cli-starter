# tag::schema-config[4] Basic Schema configuration
extend type Query {
  greeting: String @resolver
  author: String @resolver
  greet(name: String!): String @resolver
}

extend type Mutation {
  echo(message: String!): String @resolver
}
